# Gallery

This app searches images from flickr api
The Results are shown in form of grid 
Grid size is customizable
Offline support is given 
For offline support, android jetpack room is used
For networking layer, kotlin fuel is used
The Architecture followed is MVVM
There are 3 layers as data, base and app
data layer contains all the business logic
Voice search is also included
